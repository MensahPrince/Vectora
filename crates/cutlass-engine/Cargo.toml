[package]
name = "cutlass-engine"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Cutlass editing engine"

[dependencies]
cutlass-cache = { path = "../cutlass-cache" }
tracing.workspace = true
