// Cross-panel timeline state (zoom, playhead). Ephemeral panel view state
// lives in `TimelineViewState` (`ui/panels/timeline/state.slint`).
export global TimelineStore {
    in-out property <float> zoom: 3;
    in-out property <int> playhead-tick: 0;
}
