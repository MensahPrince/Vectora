[package]
name = "cutlass-commands"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Cutlass structured editor commands (import, trim, split, …)"

[dependencies]
tracing.workspace = true
