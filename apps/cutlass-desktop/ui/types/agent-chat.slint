// Assistant panel: composer, transcript bubbles, plan card, and actions.
export struct AgentChat {
    input-bg: color,
    user-message-bg: color,
    plan-bg: color,
    plan-border: color,
    send-disabled-bg: color,
    stop-bg: color,
    stop-bg-hover: color,
    error: color,
    preview-toggle-border: color,
}
