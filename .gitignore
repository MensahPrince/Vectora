/target
/dist
# cutlass-py is excluded from the workspace, so it gets its own target dir
# and development virtualenv.
/crates/cutlass-py/target/
/crates/cutlass-py/.venv/
# Demo outputs (cutlass-cli, cutlass-render's export_demo example).
/cutlass-demo.png
/cutlass-demo.mp4
# Xcode / Swift Package Manager user state and build output.
xcuserdata/
.build/
# Prebuilt Rust FFI bundle for the Swift package (158MB of static libs);
# rebuild with scripts/build-ios-xcframework.sh.
apps/cutlass-ios-macos/CutlassMobile/CutlassMobileFFI.xcframework/
# IDE metadata (Android Studio / IntelliJ).
.idea/
local-assets/
.env
.DS_Store
frames/
proxy/
.cutlass/
librry/
run.sh
venv/
crates/cutlass-py/.venv/
crates/cutlass-py/examples/trailer.mp4
