import { Project } from "../models/index.slint";

// Canonical project data lives in Rust (`src/models/domain.rs`) as HashMaps.
// Rust projects it here via `dto::project_to_slint` on startup and after
// every command mutation.
export global EditorStore {
    in-out property <Project> project;
}
